import { defineConfig } from "vite";

export default defineConfig({
  // Proxy all /api/ requests to the Node/Express server
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:3000",
        changeOrigin: true,
      },
      "/health": {
        target: "http://localhost:3000",
        changeOrigin: true,
      },
    },
  },
});